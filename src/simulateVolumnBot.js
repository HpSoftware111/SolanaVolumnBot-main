import PromptSync from "prompt-sync";

const prompt = PromptSync();

export async function simulateVolumeBot() {
    console.log("This simulate will show how much volumn will you get, how much sol will you consume.");
    const solAmount = prompt("Input the amount of ");
}